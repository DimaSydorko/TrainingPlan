import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
    position: 'absolute',
    top: 55,
    paddingVertical: 10,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-start',
    left: 0,
    zIndex: 10,
  },
  exercises: {},
})
